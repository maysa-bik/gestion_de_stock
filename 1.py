"""
            elif event.type == pygame.MOUSEBUTTONDOWN:
                # Si l'utilisateur clique sur un bouton "Ajouter produit"
                if add_button_rect.collidepoint(event.pos):
                    add_product()    
                # Si l'utilisateur clique sur un bouton "Supprimer produit"
                if delete_button_rect.collidepoint(event.pos):
                    delete_product() 
                # Si l'utilisateur clique sur un bouton "Modifier produit"
                if update_button_rect.collidepoint(event.pos):
                    update_product()      
                   """